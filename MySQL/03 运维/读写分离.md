# 介绍
 
> [!NOTE] 概述
> 把对数据库的读操作和写操作分开，以对应不同的数据库服务器。主数据库提供写操作，从数据库提供读操作，这样能有效的减轻数据库的压力


---

# 一主一从
	原理：基于二进制日志binlog实现
	缺点：主节点master宕机之后，业务系统就只能读，而不能写
# 双主双从
	介绍：
		一个主机master1用于处理所有写请求，它的从机slave1和另外一台主机master2还有它的从机slave2负责所有读请求。当master1主机宕机后。master2主机负责写请求，master1、master2互为备机