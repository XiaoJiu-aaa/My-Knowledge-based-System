[[InnoDB的逻辑存储结构.canvas]]

# 一、内存架构
缓冲池（buffer pool）：以页为单位，底层采用链表数据结构管理，分为free page（空闲页）、clean page（被使用的page，数据没有被修改过）、dirty page（数据被修改过）
更改缓冲区（change bufffer）：针对非唯一二级索引页
自适应哈希索引：
日志缓冲区


# 二、磁盘结构

# 三、后台线程
分类：
1. Master Thread
2. IO Thread
3. Purge Thread
4. Page Cleaner Thread