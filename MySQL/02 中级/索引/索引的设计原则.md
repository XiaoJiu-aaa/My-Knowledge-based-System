
> [!NOTE] Title
> 1.数据量大，查询频繁
> 2.针对常作为查询条件（where）、排序（order by）、分组（group by）操作的字段建立索引
> 3.选择区分度高的列作为索引
> 4.如果选择字符串类型的列建立索引且串较长，建立前缀索引
> 5.尽量使用联合索引，减少单列索引
> 6.控制索引的数量，索引会影响增删改的效率
> 7.如果索引列不能存储NULL，在创表时使用not null约束
