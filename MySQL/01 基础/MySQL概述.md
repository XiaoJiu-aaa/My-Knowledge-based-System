
# 一、概述

## 类型： 关系型数据库（RDBMS）

> [!NOTE] 定义
> 建立在关系模型基础上，由多张相互连接的二维表组成的数据库
>特点：
>	1.使用表存储数据，格式统一、便于维护
>	2.使用SQL语言操作，标准统一、使用方便

# 启动MySQL
#### 1.启动进程服务(管理员运行cmd)
	net start mysql80
		关闭：net stop mysql80
#### 2.客户端连接
	mysql -u root -p

[^1]: 密码：Jzx123456mysql
